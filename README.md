This repositiory explores low level programming and optimizations

For example we will reimplement the std::vector class from scractch with no outside resources.

We will then "battle-test" and time our vector implementation and comapare to vector

Then look into various optimizations
